/**
 * Firebase stub — firebase SDK is not installed in this project.
 * Auth and real-time collaboration are handled by Supabase (src/lib/supabase.ts).
 *
 * This file exists as a placeholder so any future imports don't break.
 * To enable Firebase, run: npm install firebase
 * Then replace this file with the full implementation.
 */

export const isFirebaseConfigured = false;
export const firebaseApp  = null;
export const auth         = null;
export const db           = null;
